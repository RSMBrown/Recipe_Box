<%= form_with model: [@recipe, @recipe.build_rating] do |form| %>
    <div>
        <%= form.label :rating %><br>
        <%= form.text_area :body %>
    </div>

    <div>
        <%= form.submit %>
    </div>
<% end %> 