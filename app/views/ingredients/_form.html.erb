<%= form_with model: @ingredient do |form| %>
        <div>
            <%= form.label :Ingredient %><br>
            <%= form.text_area :body %>
        </div>

        <div>
            <%= form.submit %>
        </div>
<% end %> 